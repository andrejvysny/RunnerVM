# Current State

Last verified: 2026-08-27 21:12 (local)

- **Branch:** `master` == `origin/master` at `a771905` (clean; untracked `AGENTS.md` — not written by this session — and `.claude/` agent worktrees).
- **Changed this session (all committed, 74ecb12 … a771905):** `Sources/Orchestration/{InstanceManager,RunnerSessionObserver,RunnerSessionManager,RunnerSessionRecovery,InstanceReuse,InstanceGuestAgent,OrchestratorTick,DemandProvider,ScaleSetDemandProvider,HostModeControl,LifecycleEventLog,ImagePulling,Mapping,GitHubGateway,MetricsGitHubRequestObserver,DaemonRuntime}.swift`; `Sources/Orchestration/Build/{ImageBuilderRecovery,BuilderWorker,ImageBuilder,BuildContextPacker,BaseImageCache*,BuildHooks,BuildArgResolver,BuildMapping}.swift`; `Sources/Persistence/{Migrations/Migrator (v3),Repositories/{InstanceRepository,ImageBuildRepository,OperationRepository}}.swift`; `Sources/RunnerCore/{Models/RunnerProfileConfig,Configuration/ProfileValidation,Errors/ImageBuildError,Doctor/*}.swift`; `Sources/GitHubControl/{HTTP/GitHubHTTPClient,HTTP/GitHubRequestObserver,ScaleSet/ActionsServiceConnection,API/GitHubRunnersAPI,ControlPlane/*}.swift`; `Sources/runnerctl/{Doctor*,ImageBuildCommand,BuildCommands,ImageTransferCommands,ScaleSetCommands}.swift`; `Sources/DaemonAPI/{BuildDTOs,CatalogueDTOs,ScaleSetDTOs,DaemonService}.swift`; `GuestAgent/internal/cleanup/{home,cleanup}.go`; `scripts/{live-github-e2e,live-builder-e2e,live-builder-faults,qualify-host}.sh`, `scripts/lib/live-common.sh`, `scripts/tests/qualify-host-test.sh`; `.github/workflows/{ci,e2e}.yml`; docs (`status`, `verification`, `image-build`, `install`, `live-integration`, `qualification`, `design/build-secrets`, `db_schema_v3.sql`), `CHANGELOG.md`, `PROVENANCE.md`, `Proto/{daemon_api,guest_agent}.md`, `TODO.md`.
- **Build/test:** `swift build && swift test --parallel` → 1314 tests / 167 suites pass (1 known issue: `mknod` needs root); `cd GuestAgent && go vet ./... && go test -race ./...` → pass; `shellcheck scripts/*.sh scripts/lib/*.sh scripts/tests/*.sh`, `actionlint`, `bash scripts/tests/{install-test,qualify-host-test,build-ubuntu-image-test}.sh` → pass. CI: green at `85dcdcf` (run 33105752718); `58acb8e`/`a771905` runs were in progress at hand-off — check `gh run list --branch master --workflow ci`.
- **Live (this Mac, `runnerd --foreground --state-dir ~/runnervm-dev --socket-dir /tmp/rvm`):** GitHub lifecycle matrix 11/11 pass (long-job skipped by operator); builder→GitHub→GHCR pipeline 5/5; `scripts/live-builder-faults.sh --phase all` finished: daemon converged at every phase (`booting` → `recoveryPending=yes` while the orphaned vmworker lived → `failed`/`BUILD_INTERRUPTED`; `provisioning`/`sealing` → `failed`/`BUILD_INTERRUPTED`; `queued/resolving/staging` unobservable on a warm build) but every phase was *reported* fail by the driver's single-alias image check (see HANDOFF.md). No runnerd is running after the script exits.
- **Key decisions:** teardown ordering (row before guest; instance row before session row) and event-driven test waits instead of timeouts · restart-terminalized sessions destroy their VM (`retainForDiagnosis=false`) · builds keep capacity until the worker is proven dead (`recovery_since`, schema v3, 15-min abandon) · `ARG` is non-secret, credential shapes refused · `lifecycle: reusable` needs `reuse.acknowledgeSharedHost` + guest HOME snapshot reset · no VM cancellation on unconfirmed (registration-time) demand · LaunchDaemon/reboot qualification skipped on operator decision · `long-job` scenario skipped on operator instruction.
- **Blockers:** none for code. Physical LaunchDaemon qualification needs the operator (sudo + reboots). Startup stall (silent minutes after "demand provider") seen twice, not reproduced on demand — retries now logged.
