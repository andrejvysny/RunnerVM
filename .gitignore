.build/
*.xcodeproj
*.xcworkspace
.swiftpm/
DerivedData/
GuestAgent/bin/
*.img
*.iso
.DS_Store

# Reference clone of openai/tart (own git repo); never part of RunnerVM
tart/

# Claude Code agent worktrees: ~10 GB of throwaway clones, never part of the repo
.claude/
